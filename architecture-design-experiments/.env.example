# Common
NODE_ENV=development
PORT=3000
CORS_ORIGINS=http://localhost:3000,http://localhost:4000
RATE_LIMIT_LOGIN_PER_MIN=10
RATE_LIMIT_REFRESH_PER_MIN=20

# JWT / Keys
JWT_ISS=https://api.local
JWT_AUD=https://api.local
ACCESS_TTL=15m
REFRESH_TTL_SECONDS=604800
INTERNAL_JWT_ISS=https://gateway.local
INTERNAL_JWT_AUD=https://services.local
JWKS_URL=http://localhost:3000/jwks
PRIVATE_KEY_PATH=./secrets/jwt.key
PUBLIC_JWKS_PATH=./secrets/jwks.json

# Database
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/login_register

# Email / Reset
RESET_TOKEN_TTL_MINUTES=30

# Service-to-service
INTERNAL_SHARED_SECRET=change-me
