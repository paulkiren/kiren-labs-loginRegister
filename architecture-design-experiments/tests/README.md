# tests

- contract/: OpenAPI snapshot and schema tests to prevent drift.
- e2e/: End-to-end flows per architecture (monolith first, then gateway path).
- perf/: k6/Artillery smoke to compare p95 across tracks.
